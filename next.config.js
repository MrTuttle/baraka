/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.dog.ceo",
      "images.unsplash.com",
      "images.pexels.com",
      "cataas.com",
    ],
  },
};

module.exports = nextConfig;
